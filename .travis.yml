language: cpp

compiler:
    - clang
    - gcc

notifications:
    email:
        on_failure: always
        on_success: always  # XXX For experimentation, set to "change" later


before_install:
    - sudo apt-get install cmake
install:
    - sudo apt-get install libprotobuf-dev protobuf-compiler libboost-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..
    - make
script:
    - make test
